// Voice transcript reads. Transcripts are returned as part of the voice call
// detail so the replay page can render the full conversation in one query.
export { findVoiceCallDetail } from "./voice-repository";
